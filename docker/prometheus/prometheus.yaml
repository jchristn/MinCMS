global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Scrape MinCMS telemetry via the OpenTelemetry Collector's Prometheus exporter.
  - job_name: mincms-otel-collector
    static_configs:
      - targets: ['otel-collector:8889']

  # Scrape Prometheus itself.
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
